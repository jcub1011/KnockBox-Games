{
  "name": "knockbox-phaser",
  "version": "0.2.0",
  "private": true,
  "description": "KnockBox networking client for Phaser. Tests cover the pure protocol logic (kb-core.js), the server-less local peer (knockbox-local.js), and the host-authoritative helper (kb-authority.js).",
  "scripts": {
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "eslint": "^10.5.0",
    "vitest": "^4.1.9"
  }
}
